<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="co.edu.javeriana.ciclipointer">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegistroActivity" />
        <activity
            android:name=".InicioActivity"
            android:label="@string/title_activity_inicio"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".RecorridosActivity" />
        <activity android:name=".AmigosActivity" />
        <activity android:name=".UsuarioDetalleActivity" />
        <activity android:name=".ViajeActivity" />
        <activity android:name=".ProgramarViajeActivity" />
        <activity android:name=".ViajeProgramadoActivity" />
        <activity android:name=".MisRutasActivity" />
        <activity android:name=".MisRutasDetalleActivity" />
        <activity android:name=".NuevoGrupalActivity" />
        <activity android:name=".NuevoGrupalDetalleActivity" />
        <activity android:name=".AmigosGrupalActivity" />
        <activity android:name=".MisGrupalesActivity" />
        <activity android:name=".MisGrupalesInvitadoActivity" />
        <activity android:name=".MapInfoActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name" />
        <activity
            android:name=".NuevaActivity"
            android:label="@string/title_activity_nueva" />
        <activity android:name=".UserAdapter" />
        <activity android:name=".SolicitudesAmistadActivity" />
        <activity android:name=".ProgramadoActivity" />

        <service
            android:name="services.ProgramadoService"
            android:exported="false" />

        <activity android:name=".NuevoProgramadoActivity" />
        <activity android:name=".MisProgramadosActivity" />
        <activity android:name=".MisProgramadosDetalleActivity" />
        <activity android:name=".MisGrupalesDetalleActivity" />
        <activity android:name=".EspecialActivity" />
        <activity android:name=".RutaEspecialActivity" />
        <activity android:name=".MapEspecialActivity" />
        <activity android:name=".InicioTurismo" />
        <activity android:name=".ProgramarViajeTurismoActivity" />
        <activity android:name=".ProgramadoTurismoActivity" />
        <activity android:name=".ProgramadosTurismoActivity" />
        <activity android:name=".ProgramadosTurismoDetalleActivity" />
        <activity android:name=".ProgrmarMarcadorActivity" />
        <activity android:name=".MarcadorMapActivity"></activity>
    </application>

</manifest>